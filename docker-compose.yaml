version: "3"
services:
  streamlit:
    container_name: streamlit
    build:
      context: .
      dockerfile: ./Dockerfile
    restart: always
    hostname: '0.0.0.0'
    ports:
      - "8501:8501"
